# CV Bullets — Draft (edit with your real numbers after running the project)

Fill in the bracketed numbers with your actual results before using these.
Each bullet maps to one of the original skill gaps, so you can mix-and-match
into different resume sections (Projects, Skills, or inline in Experience
if relevant).

1. **SQL**
   "Designed a normalized SQLite schema and wrote 20+ analytical SQL queries
   (window functions, CTEs, correlated subqueries, multi-dimensional
   aggregations) to analyze churn drivers across 7,000+ customer records,
   identifying [X]% higher churn in month-to-month vs. long-term contracts."

2. **EDA / Statistics**
   "Conducted exploratory data analysis and designed a simulated A/B test
   to evaluate a retention-offer intervention, using chi-square testing and
   confidence intervals to quantify a statistically significant
   [X percentage point] reduction in simulated churn (p < [X])."

3. **Deep Learning**
   "Built and trained a feedforward neural network from scratch in PyTorch
   (custom architecture, training loop, and regularization) to predict
   customer churn, benchmarking it honestly against XGBoost/Random Forest
   baselines and reporting AUC of [X] vs [X]."

4. **MLOps / Experiment Tracking**
   "Tracked [X]+ model training runs with MLflow, logging hyperparameters,
   metrics, and model artifacts to enable reproducible comparison across
   gradient-boosted and deep learning approaches."

5. **Deployment**
   "Deployed the best-performing churn model behind a FastAPI REST endpoint,
   containerized with Docker for portable, environment-independent serving."

6. **GenAI (differentiator)**
   "Built a retrieval-augmented generation (RAG) pipeline combining a
   classical ML churn prediction with an LLM-generated, plain-English risk
   explanation grounded in retrieved customer support context — bridging
   classical ML and generative AI in one production-style workflow."
